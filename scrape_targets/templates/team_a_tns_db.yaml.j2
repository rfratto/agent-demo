scrape_configs:
- job_name: team_a_tns_db
  static_configs:
    - targets: ['team-a:8000']
      labels:
        origin: 'team-a'
        process: 'tns_db'
remote_write:
- url: {{ secrets.remote_write_url }}
  basic_auth:
    username: {{ secrets.remote_write_username }}
    password: {{ secrets.remote_write_password }}
